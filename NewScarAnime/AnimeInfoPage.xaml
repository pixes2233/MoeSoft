<Page x:Class="NewScarAnime.AnimeInfoPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NewScarAnime"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d" 
      d:DesignHeight="768" d:DesignWidth="1366"
      Title="AnimeInfoPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ui:Button Grid.Column="0" Appearance="Transparent" BorderThickness="0" Padding="0" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10" Click="BackToHome">
                <ui:Button.Icon>
                    <ui:SymbolIcon Symbol="ArrowLeft48"  FontSize="25"/>
                </ui:Button.Icon>
            </ui:Button>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10">
                <ui:Button Appearance="Primary" Content="Bangumi网址" ToolTipService.InitialShowDelay="100" Padding="5" Click="OpenBangumiURL" >
                    <ui:Button.Icon>
                        <ui:SymbolIcon Symbol="Link48" FontSize="18"/>
                    </ui:Button.Icon>

                    <ui:Button.ToolTip>
                        <StackPanel Orientation="Horizontal" Margin="4">
                            <ui:SymbolIcon Symbol="Info12" Width="16" Height="16" Margin="0,0,4,0"/>
                            <TextBlock Text="Bangumi番剧详情" />
                        </StackPanel>
                    </ui:Button.ToolTip>
                </ui:Button>
                
                <ui:Button x:Name="RefreshBtn" Appearance="Primary" Content="刷新数据" Padding="5" Click="RefreshAnimeData">
                    <ui:Button.Icon>
                        <ui:SymbolIcon Symbol="ArrowClockwise48" FontSize="18"/>
                    </ui:Button.Icon>
                </ui:Button>

                <ui:Button Appearance="Primary" ToolTipService.InitialShowDelay="100" Content="本地视频链接" Padding="5" VerticalAlignment="Top" Click="AddLocalLinkView">
                    <ui:Button.Icon>
                        <ui:SymbolIcon Symbol="Link48" FontSize="18"/>
                    </ui:Button.Icon>

                    <ui:Button.ToolTip>
                        <StackPanel Orientation="Horizontal" Margin="4">
                            <ui:SymbolIcon Symbol="Info12" Width="16" Height="16" Margin="0,0,4,0"/>
                            <TextBlock Text="选择存放番剧视频的文件夹" />
                        </StackPanel>
                    </ui:Button.ToolTip>
                </ui:Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="362"/>
                <RowDefinition Height="8*"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <Border x:Name="AnimeCover" Grid.Row="0" Grid.Column="0" CornerRadius="25"  Height="342" Width="240" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10">
                <Border.Background>
                    <ImageBrush ImageSource="{Binding AnimeDetails.AnimeCover}" Stretch="UniformToFill"/>
                </Border.Background>
            </Border>

            <Grid Grid.Row="0" Grid.Column="1" Height="352" VerticalAlignment="Bottom">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="8*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding AnimeDetails.Title}" FontSize="36" FontWeight="Bold" Margin="10,10,10,0" TextWrapping="Wrap" VerticalAlignment="Top"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding AnimeDetails.TitleCN}" FontSize="18" FontWeight="Bold" Margin="10,0,0,0" TextWrapping="Wrap" VerticalAlignment="Top"/>
                <Border Grid.Row="2" Grid.Column="0" CornerRadius="25" Margin="10">
                    <ScrollViewer VerticalScrollBarVisibility="Hidden">
                        <TextBlock Text="{Binding AnimeDetails.Summary}" FontSize="15" TextWrapping="Wrap" Foreground="Gray"/>
                    </ScrollViewer>
                </Border>
            </Grid>

            <Grid Grid.Row="1" Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Width="240" Margin="10" Background="#1A808080" CornerRadius="25" Padding="0" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="{Binding AnimeDetails.BeginDate}" FontSize="15" HorizontalAlignment="Left" TextWrapping="Wrap" Margin="5"/>
                        <TextBlock Grid.Row="1" Text="{Binding AnimeDetails.TotalEP}" FontSize="15" HorizontalAlignment="Left" TextWrapping="Wrap" Margin="5"/>
                        <TextBlock Grid.Row="2" Text="{Binding AnimeDetails.Update}" FontSize="15" HorizontalAlignment="Left" TextWrapping="Wrap" Margin="5"/>
                        <TextBlock Grid.Row="3" Text="{Binding AnimeDetails.Director}" FontSize="15" HorizontalAlignment="Left" TextWrapping="Wrap" Margin="5"/>
                        <TextBlock Grid.Row="4" Text="{Binding AnimeDetails.Writer}" FontSize="15" HorizontalAlignment="Left" TextWrapping="Wrap" Margin="5"/>
                        <TextBlock Grid.Row="5" Text="{Binding AnimeDetails.Studio}" FontSize="15" HorizontalAlignment="Left" TextWrapping="Wrap" Margin="5"/>
                        <TextBlock Grid.Row="6" Text="{Binding AnimeDetails.NowEP}" FontSize="15" HorizontalAlignment="Left" TextWrapping="Wrap" Margin="5"/>
                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="1" Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="8*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Column="0" Grid.Row="0" Margin="10" Background="#1A808080" CornerRadius="25" Padding="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <ui:TextBlock Grid.Row="0" x:Name="LocalLinkError" Text="你尚未添加视频地址" Visibility="Collapsed"/>

                        <ItemsControl ItemsSource="{Binding Files}" Margin="5">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Vertical"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <ui:CardAction Height="50" HorizontalAlignment="Stretch" Margin="0,0,0,15" Content="{Binding Name}" VerticalContentAlignment="Center" FontSize="15" Click="CardAction_Click">
                                            <ui:CardAction.Template>
                                                <ControlTemplate TargetType="{x:Type ui:CardAction}">
                                                    <Border x:Name="CardBorder"
                                                            Background="White"
                                                            CornerRadius="20"
                                                            Padding="7">
                                                        <ContentPresenter
                                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                            Content="{TemplateBinding Content}"
                                                            ContentTemplate="{TemplateBinding ContentTemplate}" />
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <!-- 鼠标悬停触发 -->
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="CardBorder" Property="Background" Value="SteelBlue"/>
                                                        </Trigger>
                                                        <!-- 按下触发 -->
                                                        <Trigger Property="IsPressed" Value="True">
                                                            <Setter TargetName="CardBorder" Property="Background" Value="DarkBlue"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </ui:CardAction.Template>

                                            <ui:CardAction.Style>
                                                <Style TargetType="ui:CardAction">
                                                    <Setter Property="Foreground" Value="Black"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding LastWatchStatus}" Value="true">
                                                            <Setter Property="Foreground" Value="LightPink"/>
                                                        </DataTrigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ui:CardAction.Style>
                                        </ui:CardAction>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Border>

                <StackPanel Grid.Column="1" Margin="10">
                    <ui:TextBlock Text="指定打开方式" FontWeight="Bold"/>
                    <ui:TextBlock Text="用PotPlayer打开" />
                    <ui:ToggleSwitch x:Name="PotOpenMode" OffContent="Off" OnContent="On" IsEnabled="False"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Page>
